import createNote from '../assets/styles/createnote.module.css';

const CreateNotes = () => {
  return (
    <section className={createNote.container}>
      <div className={createNote.navbar}>
        
      </div>
    </section>
  );
}
 
export default CreateNotes;